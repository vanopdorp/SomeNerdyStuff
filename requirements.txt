# Install torch + torch_xla for TPU v5e first, matching versions, e.g.:
#   pip install torch~=2.5.0
#   pip install torch_xla[tpu]~=2.5.0 \
#       -f https://storage.googleapis.com/libtpu-releases/index.html
# (check https://github.com/pytorch/xla for the current recommended pair
#  of torch / torch_xla versions -- they must match).
datasets>=2.19
tiktoken>=0.7
numpy>=1.26